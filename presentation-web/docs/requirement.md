I have prepared the presentation content in @docs/content.md.

Create an HTML page to help me present a long Markdown file clearly with the following features:

1. Expand/collapse functionality for H2 sections
2. When pressing Next/Back, highlight one H3 block at a time
3. When navigating Next/Back:
    - If it reaches the first/last H3 in an H2 section, collapse the current H2
    - Then expand the previous/next H2 section accordingly

The solution should work with any Markdown file (e.g., index.md) in the docs folder.